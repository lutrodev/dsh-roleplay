# Roleplay 角色卡

角色卡用于保存故事角色的身份、性格、背景、场景、对话示例、头像和开场白。插件提供角色卡资料库，可创建、编辑、删除角色卡，并为不同对话选择角色和开场。

支持 Character Card V1、V2、V3 的 JSON 与 PNG 导入，也支持默认开场、备用开场和内嵌世界书。选择文件后，导入按钮会持续显示进度状态并避免重复选择，直到本次导入完成。角色卡详情可以把当前已保存的修改与最新关联世界书导出为 Character Card V3 PNG；没有头像时使用透明 PNG 容器。导入时隔离的可执行提示不会重新写入导出文件，头像也会继续移除原始卡片和图片元数据。
