import Schema from '@deepseek-ai/schemastery'

export const name = 'rp-message-avatar'
export const inject = []
export const Config = Schema.object({})

/** Mount the browser-owned message avatar projection. */
export function apply() {}
