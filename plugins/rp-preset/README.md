# Roleplay 创作预设

创作预设用于保存可复用的故事规则与写作任务，例如用户参与方式、角色自主性、连续性、剧情推进、自检和输出要求。

可以从空白预设开始，也可以复制内置的通用示例，再自由新增、删除、排序和调整栏位。每个栏位还能设置是否默认使用 `<section>`／`<item>` 分组标签；会话首次使用时继承该值，之后可在“回复资料”中独立调整。插件还支持把 SillyTavern Chat Completion 预设按语义转换为创作预设与独立文风。
