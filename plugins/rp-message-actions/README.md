# Roleplay 消息操作

为 Roleplay 对话提供复制、行内编辑、删除、重新生成和从指定位置新建对话等操作。已中断或失败但仍有可读正文的回复也可以继续处理。

删除或重新生成会同步调整当前可见故事和相关会话变量，但不会撤销已经保存到共享资料库的角色卡、世界书、人设、预设或文风。删除助手消息后，触发该回复的用户消息、头像与操作条继续保留，可以直接编辑、删除或重新生成；编辑带图片的用户消息时，原附件会保留。

剧情提交失败时，界面保留与失败提交配对的正文和恢复操作；后续模型产生的空白占位或确认文本不会替代这段可恢复正文。
