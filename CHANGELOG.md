# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## Unreleased

- 新增可自定义快捷回复插件：输入栏以原生工具样式显示前三项常用内容，支持在光标或选区处插入、成对符号包裹；新增、编辑、排序、删除和持久化统一放在 Roleplay 的“快捷回复”功能卡中，插入后不会自动发送。

## 0.1.4 - 2026-08-27

- 角色卡与世界书导入期间显示持续加载状态并防止重复选择文件，导入或新建的资料按加入时间倒序排列，后续编辑不会改变顺序。
- State 提交参数无效时保留已经生成的回复，返回具体到变更项的字段纠错，并允许在同一轮只重试原子提交。
- 消息恢复界面优先保留与失败提交对应的正文，不再被后续空白占位或确认文本替代。
- 扩展 MVU 路径兼容：规范化点号、括号、通配符、占位符与有限并集路径，支持安全的 Zod 声明子集，并对开放路径使用 `schema-only` 而不是生成字面通配键。

## 0.1.3 - 2026-08-26

- Roleplay Agent 模式新增 Harness 极简模式同款的持久终端与 `str_replace_editor`，用于检查、读取和按需编辑工作区文件；Chat 模式继续屏蔽这两项能力。
- 修正“我的人设”描述快捷项的插入位置：新字段另起一行，光标停在字段名后的冒号处，便于直接填写内容。

## 0.1.2 - 2026-08-26

- Roleplay 管理接口改用 Harness 的可信 Host 边界，允许通过 Harness 明确信任的远端地址管理资料、消息操作和 Roleplay 设置。
- Roleplay 设置改由服务端执行带版本校验的写入，避免远端界面绕过统一校验或覆盖并发修改。
- 默认人设占位名改为“用户角色”，不会改写已有或用户编辑过的人设。
- 精简内置创作预设的结尾指导，同时保留开放接续、因果连续和用户行动空间要求。

## 0.1.1 - 2026-08-26

- 角色卡详情页新增 Character Card V3 PNG 导出，导出内容以当前已保存的角色资料为准。
- 导出时合并最新关联世界书，并继续隔离可执行提示、清理原始卡片和图片元数据。
- 支持读取 PNG 中标准 `ccv3` 数据块，同时保留对既有 `chara` 数据块的兼容。

## 0.1.0 - 2026-08-25

- 将 Roleplay 插件套件整理为独立的 `dsh-roleplay` pnpm workspace。
- 移除本机开发环境、调试入口和外部社区夹具耦合，仓库依赖改用已发布的 Harness 包。
- 增加开源许可、贡献指南、安全策略和持续集成配置。
- 首个 Roleplay 插件套件版本。
